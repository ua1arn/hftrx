Как базовые функции цифровой обработки использовать предоставленные https://github.com/ARM-software/CMSIS-DSP.git.
По возможности использовать макроопределение ARM_MORPH для функций и типорв данных из библиотеки cmsis-dsp.
Учитывать type promotion языка Си - не указывать тип если константа приведется к вещественным.
Вместо типов float32_t  или float64_t использовать тип FLOAT_t.
Вместо функций библиотеки math.h использовать описанные в https://github.com/ua1arn/hftrx/blob/master/inc/dspdefines.h.
Комментарии в формируемых исходных текстах писать на английском языке.
Внешние переменные и функции в формируемых исходных текстах делать с классом памяти static.
Стандартные заголовочные файлы в исходный текст не включать.
arm_sqrt_f64 нет в библиотеке cmsis-dsp, использовать SQRTF из dspdefines.h.
arm_sin_cos_f32 использовать без ARM_MORPH
При использовании ARM_MORPH не требуется указывать суффикс типа обрабатываемых данных.
Разделить каналы приёма и передачи - и использовать функции с передачей контекста, а не внешние переменные.
Переменные, не модифицируемые в дальнейшем по программе, снабжайте атрибутом const. Это улучшает читаемость.
Для хранения Boolean используйте тип int
Для обеспечения нахождения значений типа FLOAT_t в нужном диапазоне использовать комбинацию из FMAXF и FMINF

Объедините всё в один файл и перешлите мне его в виде ZIP архива.
Перешлите, пожалуйста, мне результат в виде ZIP архива.
Выдайте пожалуйста сюда текст Patyon-скрипта, генерирующего функцию.
Удобно будет, если выложите полный текст функции на Pastebin или аналогичный сервис.
Сcылку на скачивание в виде base64 текста сюда пожалуйста.


